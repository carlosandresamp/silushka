import Root, {
  NumberFieldRootBaseProps as RootBaseProps,
  NumberFieldRootProps as RootProps,
  NumberFieldRootVariantProps as RootVariantProps,
} from "./Root";
import styles from "./styles";

export type { RootBaseProps, RootProps, RootVariantProps };

export { Root, styles };

export const NumberField = Root;

export default NumberField;

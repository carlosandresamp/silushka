import Root, {
  ChipRootBaseProps as RootBaseProps,
  ChipRootElementProps as RootElementProps,
  ChipRootProps as RootProps,
  ChipRootSharedElementProps as RootSharedElementProps,
  ChipRootVariantProps as RootVariantProps,
} from "./Root";
import styles from "./styles";

export type { RootBaseProps, RootElementProps, RootProps, RootSharedElementProps, RootVariantProps };

export { styles };

export const Chip = Root;

export default Chip;
